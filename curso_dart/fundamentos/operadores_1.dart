main() {
// Operadores Aritméticos
//Binarios: + - * / %

  int a = 7;
  int b = 3;

  int resultado = a + b;

  // print('Soma: $resultado');
  // print("Subtração: ${a - b}");
  // print("Multiplicação: ${a * b}");
  // print("Divisão: ${a / b}");
  // print("Resto: ${a % b}");
  // print('Precedência: ${a + (b * a) - (b / a) - (a % b)}');

  //Operadores Lógicos

  bool isFragile = true;
  bool isExpensive = false;

  // print("E: ${isFragile && !isExpensive}"); //AND -> E
  // print("OU : ${isFragile || isExpensive}"); //OR -> OU
  // print("XOR : ${isFragile ^ isExpensive}"); //XOR -> OU EXCLUSIVO
  // print("NOT : ${!isFragile}"); //NOT/prefix/unary
  // print("!! : ${!!isFragile}"); //NOT2X
}
